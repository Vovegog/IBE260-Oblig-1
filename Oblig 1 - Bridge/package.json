{
  "dependencies": {
    "express": "^4.18.2",
    "typescript": "^5.3.3"
  },
  "name": "main.ts",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npx tsc && node main.js",
    "dev": "node ./node_modules/nodemon/bin/nodemon.js -e ts --watch ./src --exec \"npm run compile\" || exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "nodemon": "^3.0.3"
  }
}
